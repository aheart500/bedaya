import { Button, TextField } from "@mui/material";

const IndexPage = () => {
  return (
    <div>
      <TextField variant="outlined" label="Nasser" />;
      <img src="/images/logo.png" />
    </div>
  );
};

export default IndexPage;
